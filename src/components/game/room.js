import React from 'react';
import styled from 'styled-components';

const StyledEmptyRoom = styled.div`
  width: 4%;
  height: 4%;
`;

function Room() {
  return <StyledEmptyRoom />;
}

export default Room;
